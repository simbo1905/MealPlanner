prompts:
  - id: hello-world
    label: Hello World Test
    prompt:
      - role: user
        content: "Say 'Hello, World!' and nothing else."

providers:
  - id: mistral:mistral-tiny-latest
    config:
      apiKey: $MISTRAL_API_KEY
      temperature: 0

tests:
  - description: "Simple hello world test - using $MISTRAL_API_KEY without braces"
    vars: {}
    assert:
      - type: contains
        value: "Hello"

import { describe, test } from 'vitest'

describe('UUIDGenerator', () => {
  test.todo('generates increasing UUIDs within the same millisecond')
  test.todo('includes random component for uniqueness across sessions')
})

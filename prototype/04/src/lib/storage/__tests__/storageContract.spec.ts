import { describe, test } from 'vitest'

describe('StorageAdapter contract', () => {
  test.todo('satisfies CRUD semantics across adapters')
  test.todo('supports append-only day event logs')
  test.todo('handles optional change streams where implemented')
})

import { describe, test } from 'vitest'

describe('rebuildDayMeals', () => {
  test.todo('replays add and delete events to final meal list')
  test.todo('keeps chronological order based on occurredAtEpochMs')
})

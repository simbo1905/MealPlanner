rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads/writes in development (replace with auth rules in production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
